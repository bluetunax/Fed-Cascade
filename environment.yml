# /environment.yml
name: cascade
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - fastapi>=0.109.0
  - uvicorn>=0.27.0
  - sqlmodel>=0.0.14
  - httpx>=0.26.0
  - jinja2>=3.1.3
  - plotly==5.18.0
  - pandas==2.1.3
  - pydantic-settings>=2.2.1
  # pip is included just in case you ever need a package not on conda-forge
  - pip